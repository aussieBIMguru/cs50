///** Normal means of terminal
///code name.c = > makes new code
///make name.c = > compiles code to program
///d dirPath = > changes directory
/// ./name = > run name
/// debug50 ./name = > debug at selected line in gutter (cs50 specific)


///** Using clang
///clang -o name name.c = > same as make (-o = output)
///clang -o name name.c -lcs50 = > same as make, using cs50 library (-o = output, -l = library)

int main(void)
{
    // Do nothing
}