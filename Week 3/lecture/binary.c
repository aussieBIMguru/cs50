#include <stdio.h>
#include <cs50.h>

// Hint the compiler
int search_binary(int array[], int size, int find);

// Main
int main(void)
{
    // Make the array and finding
    int doors = 7;
    int find  = 1;
    int lockers[] = {1,5,10,20,50,100,200};
}